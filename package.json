{
  "name": "calculadora-vidrio-espejo",
  "private": true,
  "version": "1.0.0",
  "description": "Optimizador de cortes (vidrio y espejo) - PWA + APK (Capacitor).",
  "type": "module",
  "scripts": {
    "serve": "python3 -m http.server 8000",
    "sync:web": "node scripts/sync-web.mjs",
    "cap:sync": "npm run sync:web && npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}

